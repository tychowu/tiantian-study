/**
 * 版本號：MAJOR.MINOR.PATCH
 * - 每次推送（scripts/release.py）會自動把 PATCH +1，不需要手動改。
 * - 大改版用 `--part minor`，重大改動用 `--part major`。
 * 版本號與更新日期由發版腳本寫入，這裡只放常數給畫面顯示。
 */
export const APP_VERSION = "1.2.4";
export const APP_VERSION_DATE = "2026-09-17";
